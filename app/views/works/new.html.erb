<% provide(:title, "New Work") %>
<h1>New Work</h1>

<%= form_for(@work) do |f| %>
  <% if @work.errors.any? %>
    <%= render 'shared/error_messages' %>
  <% end %>
  <%= f.label :content %><br>
  <%= f.text_field :content %>
  <%= f.submit "Create work" %>
<% end %>
